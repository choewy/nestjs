export enum RedisPubSubChannel {
  Host = 'host',
  Guest = 'guest',
}

export enum HostRedisSubSubject {
  Message = 'redis-sub.host.message',
}

export enum GuestRedisSubSubject {
  Message = 'redis-sub.guest.message',
}
