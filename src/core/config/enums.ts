export enum NodeEnv {
  TEST = 'test',
  LOCAL = 'local',
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
}

export enum ConfigToken {
  SERVER = 'server',
  AWS = 'aws',
  AWS_SQS = 'aws-sqs',
  MYSQL = 'mysql',
}
